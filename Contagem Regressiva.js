let i = 10;

// Enquanto i for maior ou igual a 1, o loop continua
while (i >= 1) {
    console.log(i);  // Imprime o valor atual de i
    i--;             // Decrementa i em 1 a cada iteração
}

// Quando i se torna menor que 1, o loop termina
